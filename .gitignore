# Node, from local testing only. Never part of the site.
node_modules/
package-lock.json
package.json

# macOS
.DS_Store

# Command Center private to-do. LOCAL ONLY, never publish.
# Holds this-week, personal, family, and job-search items.
command/todo.html

# Private DMA Discord webhook URL - never publish
discord/.webhook.local

# Private morning to-do, never publish
TOMORROW.md

# Private working drafts, never publish
drafts/
